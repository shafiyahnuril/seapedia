import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({ message: "GET /api/delivery - belum diimplementasi" });
}

export async function POST() {
  return NextResponse.json({ message: "POST /api/delivery - belum diimplementasi" });
}
